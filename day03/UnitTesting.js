// Unit test for reverseArray function

// Test case: Should return an empty array when the input is an empty array
/*
test('reverseArray returns empty array when input is empty array', () => {
  const input = [];
  const expectedOutput = [];
  const actualOutput = reverseArray(input);
  expect(actualOutput).toEqual(expectedOutput);
});
*/
// Unit test for reverseArray function with objects as elements
/*
test('reverseArray preserves order of objects in array', () => {
  const input = [
    { name: 'John', age: 25 },
    { name: 'Jane', age: 30 },
    { name: 'Michael', age: 35 }
  ];
  const expectedOutput = [
    { name: 'Michael', age: 35 },
    { name: 'Jane', age: 30 },
    { name: 'John', age: 25 }
  ];
  const actualOutput = reverseArray(input);
  expect(actualOutput).toEqual(expectedOutput);
});
*/